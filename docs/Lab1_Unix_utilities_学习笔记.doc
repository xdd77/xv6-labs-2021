<!DOCTYPE html><html><head><meta charset="utf-8"><title>Lab1 Unix utilities</title><style>
body{font-family:"Microsoft YaHei",Arial,sans-serif;line-height:1.65;margin:32px;color:#222}h1{color:#1f4e79;border-bottom:2px solid #1f4e79;padding-bottom:6px}h2{color:#2f75b5}h3{color:#365f91}code,pre{font-family:Consolas,monospace;background:#f3f6f8}pre{padding:12px;border-left:4px solid #5b9bd5;white-space:pre-wrap}table{border-collapse:collapse;width:100%;margin:12px 0}th,td{border:1px solid #888;padding:7px;vertical-align:top}th{background:#1f4e79;color:white}.note{background:#eaf2f8;border-left:4px solid #1f4e79;padding:10px}.check{background:#fff2cc;padding:10px}li{margin:4px 0}.small{color:#666;font-size:90%}</style></head><body>
<h1>xv6 Lab1：Unix utilities 学习笔记</h1>
<p><b>适用：</b>MIT 6.S081 2021 / util 分支</p>
<p class="note"><b>依据：</b>严格按照官方 util lab 的五项任务与提示整理，并结合当前仓库 util 分支中的 <code>sleep.c</code>、<code>pingpong.c</code>、<code>primes.c</code>、<code>find.c</code>、<code>xargs.c</code> 与 Makefile 复盘。目标不是增加额外功能，而是解释实现路径。</p>
<h2>1. 实验总目标</h2>
<p>官方对 Lab1 的定位是“熟悉 xv6 及其系统调用”。五个程序都运行在用户态，主要工作不是修改内核，而是把已有系统调用组合成完整程序。</p>
<table><tr><th>任务</th><th>官方提示的核心动作</th><th>训练点</th></tr>
<tr><td>sleep</td><td>检查参数、atoi、调用 sleep、exit</td><td>最小用户程序和系统调用入口</td></tr>
<tr><td>pingpong</td><td>两条 pipe、fork、交换一个字节</td><td>父子进程与文件描述符</td></tr>
<tr><td>primes</td><td>按需创建进程流水线</td><td>递归进程、EOF、资源释放</td></tr>
<tr><td>find</td><td>参考 ls.c 递归读取目录</td><td>目录项、路径、递归</td></tr>
<tr><td>xargs</td><td>逐行读 stdin，fork/exec/wait</td><td>argv 构造与程序替换</td></tr></table>
<h2>2. 开始前的官方路线</h2>
<pre>git checkout util
git status
make clean
make qemu</pre>
<ul><li>先读 xv6 book Chapter 1。</li><li>参考 <code>user/echo.c</code>、<code>grep.c</code>、<code>rm.c</code> 的参数处理方式。</li><li>用 <code>git diff</code> 观察自己的修改。</li><li>主要新增用户程序和 Makefile UPROGS，不需要为完成 Lab1 修改 kernel。</li></ul>
<h2>3. sleep：最小系统调用程序</h2>
<h3>思考顺序</h3><ol><li><code>argc</code> 必须为 2，否则打印错误。</li><li><code>argv[1]</code> 是字符串，用 <code>atoi</code> 转换。</li><li>调用公开用户接口 <code>sleep(ticks)</code>，而不是直接调用内核的 <code>sys_sleep</code>。</li><li>用 <code>exit()</code> 结束。</li></ol>
<pre>if(argc != 2){
  fprintf(2, "Usage: sleep &lt;ticks&gt;\n");
  exit(1);
}
int ticks = atoi(argv[1]);
sleep(ticks);
exit(0);</pre>
<p class="check"><b>检查：</b><code>sleep.c</code> 不包含内核实现。用户函数经系统调用 stub 执行 <code>ecall</code>，内核中的 <code>sys_sleep()</code> 才真正等待 tick。</p>
<h2>4. pingpong：父子进程如何通信</h2>
<p>管道是单向字节流，所以父到子、子到父各一条：</p>
<pre>int p_c[2], c_p[2];
pipe(p_c);   // parent -&gt; child
pipe(c_p);   // child -&gt; parent
int pid = fork();</pre>
<table><tr><th>进程</th><th>保留</th><th>关闭</th></tr><tr><td>父</td><td>p_c[1] 写，c_p[0] 读</td><td>p_c[0]，c_p[1]</td></tr><tr><td>子</td><td>p_c[0] 读，c_p[1] 写</td><td>p_c[1]，c_p[0]</td></tr></table>
<p>fork 会复制文件描述符表。关闭无用端不是美化代码：只要某个写端仍被任何进程持有，读端就不能据此确认 EOF。</p>
<pre>父 write ping -&gt; 内核 pipe 缓冲区 -&gt; 子 read
子 write pong -&gt; 内核 pipe 缓冲区 -&gt; 父 read</pre>
<h2>5. primes：递归进程流水线</h2>
<p>官方要求第一个进程发送 2..35；每一级进程读取第一个数作为本级素数，只把不能整除的数写给右侧新进程。</p>
<pre>sieve(left_fd):
  if read(first) == 0: exit
  print first
  pipe(next)
  fork
    parent: filter remaining numbers into next[1], close, wait
    child : sieve(next[0])</pre>
<p><b>为什么 read==0 是结束条件：</b>上一级处理完成并关闭所有写端后，管道缓冲区读空，read 返回 0。若遗漏继承来的写端，下一级可能永远阻塞。</p>
<p><b>为什么按需创建进程：</b>课程明确要求每发现一个素数才创建下一级，避免一次性消耗 xv6 有限的进程和 fd。</p>
<h2>6. find：递归目录树</h2>
<ol><li><code>open(path)</code> 和 <code>fstat</code> 判断当前对象。</li><li>普通文件：用 <code>strcmp</code> 比较名称，不能用 <code>==</code>。</li><li>目录：逐个 <code>read(struct dirent)</code>。</li><li>跳过 <code>inum==0</code>、<code>.</code> 和 <code>..</code>。</li><li>拼接子路径后递归。</li></ol>
<pre>if st.type == T_FILE:
    compare name
if st.type == T_DIR:
    for each dirent:
        skip empty, ".", ".."
        find(path + "/" + name, target)</pre>
<p>跳过 <code>.</code> 和 <code>..</code> 是递归终止条件的一部分，否则会在当前目录和父目录之间循环。</p>
<h2>7. xargs：stdin、argv 与 exec</h2>
<p>先把原命令参数复制到 <code>newargv</code>，再逐字符读取 fd 0。遇到换行后补上输入参数与结尾空指针，fork 子进程执行 exec，父进程 wait。</p>
<pre>newargv = 原 argv[1...]
read(0, &amp;c, 1)
遇到 '\n':
  newargv[next] = buf
  newargv[next+1] = 0
  fork
    child: exec(newargv[0], newargv)
    parent: wait(0)</pre>
<p>exec 成功后不会返回原子进程代码；失败才会继续执行错误处理。官方不要求真实 Unix xargs 的批量合并优化，每行执行一次即可。</p>
<h2>8. 五个任务共同串起的系统调用</h2>
<table><tr><th>系统调用</th><th>任务</th><th>作用</th></tr><tr><td>fork</td><td>pingpong/primes/xargs</td><td>建立父子执行分支</td></tr><tr><td>pipe</td><td>pingpong/primes</td><td>创建内核字节流对象</td></tr><tr><td>read/write</td><td>多个任务</td><td>通过 fd 与内核对象交换字节</td></tr><tr><td>exec</td><td>xargs</td><td>替换子进程用户程序</td></tr><tr><td>wait</td><td>primes/xargs</td><td>等待并回收子进程</td></tr><tr><td>open/fstat</td><td>find</td><td>访问并判断文件对象</td></tr></table>
<h2>9. 当前仓库相对实验骨架的变化</h2>
<ul><li>新增五个用户程序文件。</li><li>Makefile 的 UPROGS 增加 sleep、pingpong、primes、find、xargs。</li><li>实现集中于用户层，符合官方 Lab1 范围。</li></ul>
<h2>10. 测试与故障定位</h2>
<pre>./grade-lab-util sleep
./grade-lab-util pingpong
./grade-lab-util primes
./grade-lab-util find
./grade-lab-util xargs
make grade</pre>
<table><tr><th>现象</th><th>优先检查</th></tr><tr><td>exec xxx failed</td><td>UPROGS 与 make clean</td></tr><tr><td>pingpong/primes 卡住</td><td>是否漏关管道写端</td></tr><tr><td>find 无限递归</td><td>是否跳过 . 与 ..</td></tr><tr><td>xargs 参数错位</td><td>argv 下标与末尾 0</td></tr></table>
<h2>11. 自测问题</h2>
<ul><li>为什么用户程序调用 sleep，而不是 sys_sleep？</li><li>fork 后为什么父子都有全部 fd？</li><li>为什么一个未关闭写端会阻止 read 返回 0？</li><li>primes 为什么每一级只保存第一个数？</li><li>find 为什么不能用 == 比较文件名？</li><li>argv 为什么必须以 0 结尾？</li></ul>
<h2>12. 一句话总结</h2><p class="note">Lab1 用五个用户程序练习系统调用的组合：参数与 sleep、父子管道、递归进程流水线、目录递归、fork/exec/wait。它为后续理解 syscall 入口和每进程地址空间打下基础。</p>
<p class="small">资料：MIT 6.S081 2021 util lab；当前仓库 util 分支。</p>
</body></html>